---
name: ofd-renewal-map-dev
description: Контекст и история решений по разработке "Карта продлений ОФД" (клиенты/кассы/партнёры/коды ОФД, GitHub Pages). Архитектура, формулы метрик, найденные баги с фиксами, процесс тестирования и деплоя. Загружай перед ЛЮБОЙ доработкой, правкой виджета или багфиксом в этом репозитории — чтобы не терять цепочку решений между сессиями.
---

# Карта продлений ОФД — контекст разработки

Сайт: https://dmitrijudalov58-stack.github.io/ofd-renewal-map/
Репо: https://github.com/dmitrijudalov58-stack/ofd-renewal-map
GitHub-аккаунт для push: `dmitrijudalov58-stack`, `gh` уже авторизован на машине Димы.

## Что это и как устроено

Статический BI-инструмент для карты продлений ОФД. Zero build step — просто `<script src>` теги, без npm-бандлера в продакшене (npm нужен только для dev-тестов, см. ниже). Загружаешь XLSX-выгрузку → весь разбор и расчёт метрик идёт в браузере, ничего никуда не отправляется.

```
index.html            разметка + подключение скриптов по порядку
css/styles.css         дизайн-токены (Swiss/International: ч/б + красный акцент --brand, border-radius:0
                         везде, гротеск, uppercase-лейблы. Было "ledger" — сажево-тёмно-бирюзовый/Georgia,
                         сменили на Swiss по запросу Димы. Данные-цвета --s1/--s2/--s3/статусы не трогать)
js/parser.js            XLSX (3 листа) -> нормализованные строки, читает колонки ПО ПОЗИЦИИ (не по имени —
                         в файле задвоен заголовок "Партнер": имя партнёра на позиции 19, денежная сумма на 25)
js/metrics.js            вся бизнес-логика: buildModel + все computeX функции
js/widgets.js            рендер 29 виджетов поверх metrics.js, реестр WIDGETS{}
js/dnd.js                drag-and-drop из библиотеки на холст, экспорт-кнопка
js/export.js             CSV-экспорт (с BOM для кириллицы в Excel)
js/app.js                загрузка файла, период/as-of, склейка
js/vendor/xlsx.full.min.js  SheetJS, грузится лениво при выборе файла
test/smoke.js             Node-тест ядра метрик на реальном файле
test/browser-smoke.js     jsdom-тест всех 29 виджетов + интерактива (максимально близко к браузеру)
```

## Ключевая модель данных

- **Клиент = ИНН** (организации; ИНН физлица, если организации нет — 892/551 таких строк, статус Новый/Выдан, дата появления = "Дата создания", не "Дата активации")
- **Касса = РНМ ККТ**
- **Код ОФД = PIN** — техническая единица, три статуса: Новый/Выдан (резерв партнёра, без клиента) / Зарегистрировано (клиент активировал) / Отозвано (аннулирован, нигде больше не участвует)
- Строка выгрузки = код (PIN), НЕ клиент и НЕ касса — на этом завязана вся логика buildModel в metrics.js

### Формулы, которые нельзя менять не подумав (согласованы с Димой пошагово)

- **Отток (кассы и клиента) — формула от 2026-08-06, ЗАМЕНИЛА старую "дата окончания попала в период" везде.** Касса/клиент "спасены", если продлились не позже 30-го дня включительно после своей даты окончания. С 31-го дня, если покрытия так и нет — **отток**. **Реанимация** — новый код активировался в окне 31–91 день включительно после даты окончания; после 91-го дня без продления — потеряна окончательно. Отток клиента = отток АБСОЛЮТНО ВСЕХ его касс (та же 30/31/91-дневная проверка, но по `client.currentEnd`/`clientLapsedAt`). Реализация — `metrics.js` → `kassaChurnStatus`/`clientChurnStatus` (общий хелпер `churnStatusFromEnd`), константы `CHURN_GRACE_DAYS=30`, `REANIM_WINDOW_START_DAYS=31`, `REANIM_WINDOW_END_DAYS=91`.
- **Ретроспективная привязка к месяцу**: событие относится к месяцу ДАТЫ ОКОНЧАНИЯ (не к дате, когда стало известно), но статус "pending" (не показывается ни как отток, ни как норма), пока с даты окончания не прошло 31+ дня от `asOf` — то есть глядя на текущий месяц в июне, отток за май уже виден, а последние ~30 дней перед as-of всегда занижены (данные ещё не "созрели"). См. `computeFlow`/`computeMonthlySeries`/`computeMonthlySeriesKassas` — все принимают 4-й параметр `asOf`, обязателен для новой формулы (без него откатится на `periodEnd` как fallback, но это меняет семантику "сейчас" на "конец периода").
- **Продления кассы** = COUNT(коды со статусом "Зарегистрировано" в цепочке) − 1. НЕ полагаться на флаг "Тип активации" (в 298 из 263 675 цепочек встречается два "Первичная" подряд — флаг иногда врёт)
- **Воронка (B4)**: Активировано = статус "Зарегистрировано" (эквивалент "есть ИНН клиента", взяли через статус, а не через сырое наличие ИНН — иначе отозванные-но-когда-то-активированные коды задваивались бы в "Активировано" И "Отозвано", и три столбца не сходились бы в "Создано")
- **Каналы партнёров**: Оля Зибер = поле "Партнёр" содержит "ЛК ОФД"/"ОПС ЭДО"/"ОППС ЭДО"; Лариса Пенигина = поле "Центр продаж" — один из 11 зафиксированных значений (список в `metrics.js` → `LARISA_CENTERS`); всё остальное = "Партнёры"
- **Топ партнёров по объёму (b3-top)**: изначально было "новые клиенты/кассы/продления ЗА ПЕРИОД", по фидбэку Димы упростили до общих клиентов/касс на as-of — чип у виджета соответственно "as-of", не "период"

### Режим «рокировка» (strict) — переключатель в шапке

Найден недокоммиченным в рабочей копии 2026-08-06 (не было ни в одном коммите, ни в этом файле) — оставлен и доведён до консистентности, а не откачен, так как уже был полностью подключён по всему `widgets.js`. Кнопка «Режим» в топбаре (`#strictToggle`, `js/app.js`), переключает `window.OFDState.strict` и пересобирает модель без перезагрузки файла (`buildModel(rows, {strict})`).

- **strict:true (default, "✓ Только действующие")** — клиент/касса считаются только при действующем (непрерванном) коде ОФД на as-of. Резерв физлиц (Новый/Выдан без организации) клиентами НЕ считается.
- **strict:false ("⟲ Прежний формат")** — легаси: резерв физлиц считается клиентами от даты создания (решение по 892 строкам), кассы — по «Общей дате окончания» без учёта разрывов между кодами.
- `isKassaAlive(kassa, atDate, strict)` / `kassaDeadline(kassa, atDate, strict)` в `metrics.js` — единая точка входа для "жива ли касса"/"когда дедлайн" под обоим режимами; `ctx.strict` прокинут почти во все `render()` в `widgets.js` (снэпшоты, риск-листы, партнёрские таблицы, `kassaDetailTable`).
- Формулы оттока (`kassaChurnStatus`/`clientChurnStatus`) от strict НЕ зависят — всегда interval-based через `kassaLapsedAt`/`clientLapsedAt`, это отдельная ось.

### Чип as-of / период — держать честным

Каждый виджет размечен `scope: "as-of" | "период"` в реестре `WIDGETS`. as-of = снэпшот на дату загрузки файла, верхний фильтр периода не действует. период = фильтруется диапазоном дат. **Если меняешь логику виджета так, что она перестаёт зависеть от периода (или наоборот) — обязательно поменяй chip и в `js/widgets.js`, и в библиотеке `index.html` (`.lib-item .lchip`)**. Уже была ситуация, когда я забыл это сделать при рефакторинге b3-top — поймали на ревью.

## Общие компоненты (трогаешь один — проверяй всех потребителей)

- **`kassaDetailTable(kassaArray, asOf, opts)`** (widgets.js) — таблица касс с фастфильтрами (партнёр/тариф/статус/ИНН/мин.продлений) + экспорт. Используется в b2-renewals, b2-renewdist, b2-tariff. Правишь колонки/фильтры — проверь все три.
- **`barList(rows, opts)`** — SVG horizontal bar chart (НЕ div-прогресс-бары — та версия была нечитаема, пастельная заливка на сером фоне; переписана на контрастную палитру #3987e5→#104281 по фидбэку). Используется в b1-kassdist, b1-age, b2-renewdist, b2-tariff (overview-часть), b3-channels.
- **`lineChart(months, series, opts)`** — линия+область, hover-подсказка на КАЖДОЙ точке (не только последней), `series[i].tooltips[j]` для кастомного текста подсказки. Используется в b1-new, b1-churn, b1-netgrowth.
- **`makeSortableTable(headers, rows, opts)`** — сортировка по клику: `dir[col]=true` после первого клика ВСЕГДА означает "по возрастанию", независимо от того как строки были отсортированы до клика (иначе клик по уже-отсортированной колонке визуально ничего не менял). `headers[i].html=true` — колонка НЕ экранируется (для status-pill и т.п.), остальные колонки экранируются через `esc()` автоматически.

## Найденные баги — не откатить их случайно

1. **Пробелы в названиях партнёров ломали группировки.** У части строк в поле "Партнёр" (и др. текстовых полях) — лишний пробел по краям, из-за чего "ООО Х" и " ООО Х" считались разными сущностями везде (фильтры, топы, каналы). Фикс — `cleanStr()` в `parser.js`, обрезает пробелы у всех текстовых полей при нормализации строки. Если увеличивается число "уникальных" партнёров после апдейта файла — сначала проверь этот угол.
2. **HTML не экранировался** — названия партнёров/организаций вставлялись в `<option>`/`<td>` напрямую. Фикс — `esc()` в widgets.js, применяется в `makeSortableTable` (кроме колонок с `html:true`) и во всех местах, где строим `<option>`.
3. **jsdom quirk, не баг реального браузера**: `node.querySelector(".widget-body > *")` иногда возвращает `null` в jsdom (nwsapi), хотя `.firstElementChild` на том же узле работает исправно. В `dnd.js` и тестах используется `.querySelector(".widget-body").firstElementChild` — не возвращай `> *` обратно, даже если кажется эквивалентным.
4. **Cross-realm `instanceof Date`** — если в тесте парсишь XLSX через Node-модуль `xlsx`, а `parser.js`/`metrics.js` выполняются в jsdom-window (другой realm) — `instanceof Date` между ними ломается и даёт тихие нули везде, где есть даты. В `test/browser-smoke.js` xlsx грузится через `win.eval()` в ТОТ ЖЕ jsdom-realm — так и должно быть, не откатывай на `require("xlsx")` для парсинга внутри jsdom-теста.
5. **`git push` 403 с чужим аккаунтом** — на машине был закэширован в osxkeychain credential от другого GitHub-аккаунта. Фикс — `gh auth setup-git`, прописывает per-host `credential.https://github.com.helper` override, который бьёт global `osxkeychain`.

## Тестирование

```bash
npm install   # jsdom, xlsx — только dev-зависимости, в продакшен сайт не попадают
OFD_TEST_FILE="/путь/к/реальной/выгрузке.xlsx" npm test
```

- `test/smoke.js` — ядро (parser+metrics) в чистом Node, печатает все ключевые агрегаты для визуальной сверки
- `test/browser-smoke.js` — прогоняет все 29 виджетов + интерактив (фильтры, drill-down в B4, сортировка, экспорт, rerenderAll при смене периода, раскрытие истории тарифов, рефреш графика по фильтру) в jsdom
- Тестовый файл **не в репозитории** (реальные ФИО/телефоны/ИНН клиентов), путь передаётся через `OFD_TEST_FILE`
- Известные референсные числа на файле `...ОФД Подключение 01.01.2020-30.07.2026.xlsx`, as-of 2026-07-30, **strict:true (default)**: activeClients=92983, clientsAtRisk(30d)=11890 — если видишь другие числа на этом же файле под strict:true, разбираться, не игнорировать. (До находки/доработки режима «рокировка» 2026-08-06 здесь было 10707 под legacy-формулой — не путать при сверке со старыми заметками.)

## Деплой

- GitHub Pages: `build_type=legacy` (branch-based деплой, БЕЗ workflow — сайт статический, сборка не нужна). Не переключай на `workflow`, иначе понадобится `.github/workflows/*.yml`
- После `git push` в `main` пересборка Pages запускается автоматически; форсировать вручную: `gh api -X POST repos/dmitrijudalov58-stack/ofd-renewal-map/pages/builds`
- Проверка статуса: `gh api repos/dmitrijudalov58-stack/ofd-renewal-map/pages/builds/latest --jq .status` (`building` → `built`)
- `.gitignore` блокирует `*.xlsx`/`*.xls` — защита от случайного коммита реальной выгрузки

## Рабочий процесс правок (порядок, который использовался всю сессию)

1. Правишь `metrics.js` → сразу гоняешь `node test/smoke.js` с реальным файлом, смотришь на числа глазами
2. Правишь `widgets.js`/UI → `node test/browser-smoke.js` (полный прогон, ловит runtime-ошибки и регрессии интерактива)
3. Только после зелёного smoke — `git add` конкретных файлов (никогда `-A` не глядя, проверять что не затянуло xlsx), commit, push
4. После push — дождаться `built` в Pages API, проверить фикс на живом URL curl'ом (не считать готовым по одному пушу)

## Связанные материалы

Источник правды — файлы в репозитории (переживают сессию, в отличие от scratchpad и Claude Artifacts):

- `docs/guide.html` — памятка для смежных отделов, терминология Димы (клиент=ИНН/касса=РНМ/код=PIN, стиль "Термин = определение"). Именно этот файл редактировать при правках памятки.
- `docs/planning/spec.html` — полная спека бордов/виджетов/формул (00-B4), зафиксирована до начала разработки
- `docs/planning/design-mockup.html` — первая версия живого дизайн-макета (демо-данные, одобрен Димой как визуальный язык)

Зеркала на Claude Artifacts (та же сессия, могут не пережить время — не полагаться как на источник правды, полезны только для быстрой ссылки):
- Спека: https://claude.ai/code/artifact/c96bb45e-7911-4657-9e6d-5237aab3d64f
- Дизайн-макет: https://claude.ai/code/artifact/6414ceb3-3a4d-4266-bdd5-b68433515348
- Памятка: https://claude.ai/code/artifact/e7ca4bf3-7e8a-48f3-9480-58fad0261e10

При правках терминологии/описаний виджетов — держать стиль `docs/guide.html` и стиль заметки Димы `Stars/Исследование оттока/КОНТЕКСТ.md` в его Obsidian-vault (`~/Desktop/обсидиан`): короткие декларативные определения вида **Термин = определение**, без хеджирования, конкретные названия полей выгрузки в коде. После правки `docs/guide.html` — обновить и Artifact (Artifact tool, тот же file_path, публикует на тот же URL) и PDF (см. ниже), обе версии должны быть актуальны одновременно.

## PDF-версия памятки (для рассылки без аккаунта Claude)

На машине Димы нет ни Chrome, ни wkhtmltopdf — печать HTML→PDF через headless Chromium из пакета `puppeteer` (npm), не через скилл `make-pdf` (тот рассчитан на markdown-вход и тянет инфраструктуру gstack, которая тут не нужна).

```bash
mkdir -p /tmp/pdfgen && cd /tmp/pdfgen && npm init -y >/dev/null 2>&1 && npm install puppeteer

node -e '
const puppeteer = require("puppeteer");
(async () => {
  const browser = await puppeteer.launch();
  const page = await browser.newPage();
  await page.goto("file://" + "/Users/dmitrij/Desktop/Агенты/ofd-renewal-map/docs/guide.html", { waitUntil: "networkidle0" });
  await page.emulateMediaType("screen");
  await page.pdf({
    path: process.env.HOME + "/Downloads/Карта продлений ОФД — памятка.pdf",
    format: "A4", printBackground: true,
    margin: { top: "14mm", bottom: "16mm", left: "12mm", right: "12mm" },
  });
  await browser.close();
})();
'
```

Результат уже лежит в `~/Downloads/Карта продлений ОФД — памятка.pdf` (8 страниц, кириллица проверена через `pdftotext`). Перегенерировать этим же рецептом после любой правки `docs/guide.html`.

## Статус на конец сессии (2026-08-03)

Всё сделано и задеплоено: сайт на GitHub Pages (проверен curl, 200 OK), 25 виджетов, все фидбэк-раунды закрыты (фастфильтры, воронка, каналы, barList на SVG, hover на нетто-приросте), памятка в терминологии Димы — и как Artifact, и как файл в репо, и как PDF в Downloads.

## Статус на конец сессии (2026-08-06)

- **GitHub Pages временно ОТКЛЮЧЕН** по просьбе Димы (`gh api -X DELETE repos/.../pages`) — публичная ссылка 404. Смотрит локально (`file://.../index.html` или `python3 -m http.server`). Включать обратно только по явной просьбе — конфиг для восстановления: `build_type=legacy, branch=main, path=/`.
- Найдена и доведена до консистентности недокоммиченная работа **режим «рокировка» (strict)** — см. раздел выше. Была в рабочей копии до этой сессии, без документации и коммита.
- **Новая формула оттока (30/31/91 дней)** заменила старую везде — см. раздел "Формулы" выше. Затронуты: `computeFlow`, `computeMonthlySeries`, новая `computeMonthlySeriesKassas`, `clientsChurned` (новая), `computePartnerFlow` (новая).
- **4 новых виджета** (25 → 29): `b1-churned` (Клиенты к продлению после окончания — список, зеркало b1-risk но по уже случившемуся оттоку), `b2-netgrowth` (Нетто-прирост базы по кассам, аналог b1-netgrowth), `b3-churn-top` (Топ оттока по партнёрам, retention%), `b3-partner-eff` (Партнёр: новые/отток/% эффективности). Все зарегистрированы в `index.html` (`.lib-item`).
- **Возрастная структура базы (`b1-age`)** — было 3 кумулятивных пересекающихся бакета ("старше N лет"), стало 4 эксклюзивных когорты (0-1/1-2/2-3/3+ года), сумма = totalReal, без пересечений. `computeSnapshot().ageBuckets` — ключи сменились с `1y/2y/3y` на `0-1y/1-2y/2-3y/3y+`.
- **`kassaDetailTable`** (общий компонент b2-renewals/b2-renewdist/b2-tariff) — добавлено: клик по строке раскрывает хронологию кодов кассы (дата активации → тариф → дата окончания, паттерн как в b4-partners); `wrap._getFilteredKassas()` — новый выход для кнопки-рефреша графика.
- **`b2-tariff`/`b2-renewdist`** — добавлена кнопка «⟳ обновить график по текущему фильтру» (не авто, только по клику) через общий хелпер `chartPlusFilterableTable()`. График теперь на старте строится по ТЕМ ЖЕ данным, что и таблица (раньше график = только alive-кассы под strict, таблица = все кассы — расхождение было по умолчанию, не только при фильтрации).
- **Ресайз виджетов** (`css/styles.css` — canvas grid→flex, `.widget{resize:both}`) + сохранение размера между `rerenderAll` (`js/dnd.js`, ResizeObserver + карта `sizes[instanceId]`, иначе слетал бы при каждой смене периода).
- Референс `clientsAtRisk(30d)` в `test/browser-smoke.js` обновлён 10707→11890 (см. раздел про референсные числа — это из-за strict-режима, не из-за новой формулы оттока).
- Следующая сессия начинает не с нуля — весь контекст в этом файле.

## Статус на конец сессии (2026-08-06, третья часть)

- **Ключевая находка сессии**: майско-июньский "провал" оттока — НЕ баг формулы. Проверено параллельным расчётом старой (до-грейсовой) формулы помесячно: провал воспроизводится один-в-один (май=6, июнь=3 касс со old-style "Общая дата окончания в периоде", при норме ~2500-3000/мес). Причина — массовое компенсационное продление (инцидент с ОФД, обсуждался в июле, см. `claude/chat-ofd-code-renewal-task.md` в Obsidian-vault Димы) физически сдвинуло даты окончания у майских/июньских клиентов в август (август = 13 658 при норме ~2500-3000, размазано по датам, не единым днём — компенсация была "твоя старая дата + N дней", не единая целевая дата для всех). Реанимация тут ни при чём — `reanimated=0` в обоих месяцах, проверено.
- **as-of теперь автоматически синхронизируется с концом периода** при клике "Применить" у диапазона дат (`js/app.js`, `applyBtn` handler). Раньше требовалось двигать период и as-of раздельно — источник путаницы ("почему при сдвиге периода на сентябрь отток не меняется" — as-of не следовал за period). Отдельная кнопка `applyAsOfBtn` остаётся для сознательного разъединения (применяется ПОСЛЕ периода, переопределяет синк).
- **Баннер свежести данных** (`#freshnessBanner`, `.freshness-banner` в CSS) — если `as-of` ушёл дальше самой поздней даты, которую видела выгрузка (`computeFreshness()` в `app.js` — максимум `created`/`activated` по всем строкам), показывает явное предупреждение: цифры после этой даты — допущение (нет renewal-событий в файле не значит "не продлились", значит "выгрузка ещё не видела"). Дима осознанно принял этот компромисс — без него нельзя строить план на будущее по действующей выгрузке.
- Проверено кликами через реальный UI (jsdom + File API, не прямые вызовы функций): загрузка файла → баннер уже виден (реальное "сегодня" 06.08.2026 уже позже выгрузки 30.07.2026) → сдвиг периода на 30.09 → as-of синхронно уехал → август показал реальные 13 463 (не 0) с пометкой "неполные" (хвост месяца не дозрел), сентябрь честный 0 (вся дата ещё в грейсе), июль полностью разрешён.
- GitHub Pages включён обратно (публичная ссылка снова живая) — Дима попросил открыть всем.

## Статус на конец сессии (2026-08-06, вторая часть)

- **`as-of` теперь редактируемый** — поле в шапке рядом с фильтром периода (`#asOfInput`/`#applyAsOf`, `js/app.js`). По умолчанию = момент загрузки файла (как раньше), но выгрузка могла быть снята раньше реального "сегодня" — тогда данных о недавних продлениях в файле физически нет, и `as-of` нужно двигать на дату самой выгрузки, а не полагаться на часы браузера. Меняет `window.OFDState.asOf` и дёргает `rerenderAll()`, как и период.
- **Pending-разметка месяцев** — ключевая находка сессии: `as-of` НЕ следует за фильтром периода, он всегда "реальное сегодня" (или то, что выставил руками). Из-за этого при коротких периодах, заканчивающихся близко к `as-of`, последние ~30 дней ретроспективной формулы оттока выглядели как "отток пропал" — на деле это не 0, а "ещё не прошло 30+1 дня, статус не известен". Новая `metrics.js` → `monthResolved(monthDate, asOf)` — месяц "дозрел" только когда с ЕГО ПОСЛЕДНЕГО дня прошло 31+ день от as-of (не с первого — иначе часть кодов месяца ещё в грейсе). `widgets.js` → `monthlyFlowTable(series, ctx)` — общий рендерер помесячной таблицы (Месяц/Новые/Отток/Нетто), красит Отток/Нетто серым + подпись «неполные» для не-дозревших месяцев. Переиспользуется в `b1-netgrowth`, `b2-netgrowth`, и как sub-таблица снизу в `b3-churn-top`/`b3-partner-eff`.
- **`b3-churn-top`** — добавлена колонка «Клиентов на начало периода» (`baseAtStart`, уже считался в `computePartnerFlow`, просто не был выведен) + помесячная sub-таблица снизу (по всей базе, не по партнёрам — контекст).
- **`b3-partner-eff` переписан на кассы** (было по клиентам/ИНН, стало по кассам/РНМ — концептуальный сдвиг по просьбе Димы). Новая `computePartnerFlowKassas()` в `metrics.js`, зеркало `computePartnerFlow()` но на `model.kassas`/`kassaChurnStatus`/`kassaLapsedAt`. Колонки: касс на начало периода, новых, отток, retention. Сортировка по убыванию retention (не по объёму) — из-за этого мелкие партнёры с 0 оттоком всплывают наверх с формальными 100%, это ожидаемо, не баг.
- **Фикс скролла** — панели раскрытия (история тарифов кассы, b4-years/b4-partners) росли без ограничения высоты и обрезались `.widget{overflow:hidden}` при ручном ресайзе виджета (фича с прошлой сессии). Добавлен класс `.expand-scroll` (`max-height:240px; overflow-y:auto`) в `css/styles.css`, применён во всех трёх местах в `widgets.js`.
- Проверено в jsdom на реальных данных именно на граничном месяце (период до 30.07.2026, as-of=30.07.2026): май — дозрел (нет пометки), июнь — НЕ дозрел (30 июня + 31 день = 31 июля, as-of на день раньше) — граница совпала с ожиданием день-в-день.
- Следующая сессия начинает не с нуля — весь контекст в этом файле.
